        /* For every process. */
        int main(){
            pthread_mutex_lock(&mutex);
            StackNode *top = NULL;
            push(5, &top);
            push(10, &top);
            push(15, &top);

            int value = pop(&top);
            value = pop(&top);
            value = pop(&top);
            pthread_mutex_unlock(&mutex);
        }
    
